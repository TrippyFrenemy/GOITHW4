# Використовуємо офіційний образ Python з версією 3.x
FROM python:3.10

# Копіюємо файли додатку в контейнер
WORKDIR /app
COPY . /app

# Задаємо потрібні змінні середовища (якщо необхідно)

# Виконуємо додаток при запуску контейнера
CMD ["python", "main.py"]